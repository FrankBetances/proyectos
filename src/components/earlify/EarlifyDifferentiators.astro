---
---

<section class="differentiators-section section-padding">
  <div class="container">
    <div class="section-header">
      <span class="section-badge badge-cyan">Por qué Earlify</span>
      <h2 class="section-title">
        Características que nos hacen <span class="gradient-text">especiales</span>
      </h2>
      <p class="section-description">
        No somos una aplicación más en la tienda. Construimos tecnología sanitaria con estándares clínicos, pensada para funcionar en los entornos más exigentes.
      </p>
    </div>

    <div class="diff-grid">
      <!-- Item 1 -->
      <div class="diff-card glass-card">
        <div class="diff-icon">🔒</div>
        <h3>100% Offline & Privacidad Zero-PHI</h3>
        <p>Los datos de salud y las grabaciones nunca viajan a servidores externos. Todo se procesa y queda cifrado en el propio dispositivo de la familia y del profesional.</p>
      </div>

      <!-- Item 2 -->
      <div class="diff-card glass-card">
        <div class="diff-icon">🛡️</div>
        <h3>Rigor Sanitario & Cero Cajas Negras</h3>
        <p>VIA+ es un Software como Dispositivo Médico (SaMD Clase IIa) con análisis de voz contrastado contra Praat. El profesional siempre mantiene el control clínico.</p>
      </div>

      <!-- Item 3 -->
      <div class="diff-card glass-card">
        <div class="diff-icon">🌍</div>
        <h3>Soberanía Lingüística (7 Lenguas)</h3>
        <p>Voces neuronales y bancos fonológicos propios en Castellano, Galego (Proxecto Nós), Euskara (HiTZ), Dominicano (Quisqueya Habla), Català (projecte AINA), LatAm y US English.</p>
      </div>

      <!-- Item 4 -->
      <div class="diff-card glass-card">
        <div class="diff-icon">🐱</div>
        <h3>Trilogía Asistencial con Lúa</h3>
        <p>Unimos la consulta (VIA+), el hogar (Valeria+) y la motivación afectiva (Lúa) para transformar la rehabilitación en un juego continuo y sin ansiedad.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .differentiators-section {
    position: relative;
    padding: 100px 0;
  }

  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 4rem auto;
  }

  .section-badge {
    display: inline-block;
    padding: 0.35rem 1rem;
    border-radius: var(--radius-full);
    font-size: 0.85rem;
    font-weight: 700;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .badge-cyan {
    background: rgba(0, 242, 254, 0.1);
    border: 1px solid rgba(0, 242, 254, 0.3);
    color: var(--accent-cyan);
  }

  .section-title {
    font-size: 2.6rem;
    font-weight: 800;
    margin-bottom: 1.25rem;
  }

  .section-description {
    font-size: 1.15rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .diff-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .diff-card {
    padding: 2.25rem 1.85rem;
    border-radius: var(--radius-lg);
    background: rgba(15, 23, 42, 0.4);
    border: 1px solid var(--border-light);
    transition: transform 0.3s ease, background 0.3s ease, border-color 0.3s ease;
  }

  .diff-card:hover {
    transform: translateY(-5px);
    background: rgba(0, 242, 254, 0.03);
    border-color: rgba(0, 242, 254, 0.3);
  }

  .diff-icon {
    font-size: 2.5rem;
    margin-bottom: 1.25rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .diff-card h3 {
    font-size: 1.25rem;
    font-weight: 800;
    color: var(--text-main);
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  .diff-card p {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .diff-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
