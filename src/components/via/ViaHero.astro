---
const baseUrl = import.meta.env.BASE_URL.endsWith('/') 
  ? import.meta.env.BASE_URL.slice(0, -1) 
  : import.meta.env.BASE_URL;
---

<section class="via-hero">
  <div class="container">
    <div class="hero-top-badges">
      <span class="badge badge-cyan">SaMD Clase IIa · MDR</span>
      <span class="badge badge-purple">IEC 62304 Clase B</span>
      <span class="badge badge-emerald">ISO 14971 Risk</span>
      <span class="badge badge-amber">Telemetría Zero-PHI</span>
    </div>

    <h1 class="hero-title">
      VIA<span class="gradient-text">+</span> · Valoración Interactiva de Audición y Lenguaje
    </h1>

    <p class="hero-subtitle">
      Software como Dispositivo Médico (SaMD) de Clase IIa que actúa como herramienta de <strong>apoyo a la decisión clínica (CDSS)</strong> para audiometría, logoaudiometría, fonética y neurodesarrollo pediátrico.
    </p>

    <!-- Clinical Safety Warning Box -->
    <div class="safety-warning-box">
      <div class="warning-header">
        <span class="warning-icon">⚠️</span>
        <strong>AVISO DE SEGURIDAD CLÍNICA OBLIGATORIO</strong>
      </div>
      <p>
        VIA+ es una herramienta de apoyo a la decisión clínica. Bajo ninguna circunstancia emite diagnósticos automatizados ni sustituye el juicio del profesional sanitario. Su uso está estrictamente restringido a entornos clínicos bajo supervisión profesional directa.
      </p>
    </div>

    <div class="hero-actions">
      <a href="#modulos" class="btn-primary">
        <span>11 Módulos Clínicos</span>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </a>
      <a href="#flujo-clinico" class="btn-secondary">
        <span>Flujo Clínico CAP</span>
      </a>
      <a href="https://github.com/FrankBetances/via" target="_blank" rel="noopener noreferrer" class="btn-secondary">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"></path>
          <path d="M9 18c-4.51 2-5-2-7-2"></path>
        </svg>
        <span>GitHub</span>
      </a>
    </div>

    <!-- Clean VIA+ Medical Showcase -->
    <div class="via-gallery-grid">
      <div class="phone-card glass-card">
        <div class="card-caption">
          <span class="badge badge-cyan">Módulos de Selección Pediátrica</span>
          <h4>Análisis Vocal & Audiometría Pediátrica (Play Audiometry)</h4>
        </div>
        <img 
          src={`${baseUrl}/assets/via-app-clean.png`} 
          alt="Captura real del módulo clínico de VIA+ SaMD con Análisis Acústico de Voz, Audiometría Infantil y Verbal" 
          class="via-img" 
          width="461"
          height="491"
          loading="lazy"
          decoding="async"
        />
      </div>
    </div>
  </div>
</section>

<style>
  .via-hero {
    padding-top: 130px;
    padding-bottom: 60px;
    text-align: center;
  }

  .hero-top-badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .hero-title {
    font-size: 3.2rem;
    font-weight: 800;
    max-width: 900px;
    margin: 0 auto 1.25rem auto;
    line-height: 1.15;
  }

  .hero-subtitle {
    font-size: 1.15rem;
    color: var(--text-muted);
    max-width: 800px;
    margin: 0 auto 2rem auto;
    line-height: 1.6;
  }

  .safety-warning-box {
    background: rgba(245, 158, 11, 0.08);
    border: 1px solid rgba(245, 158, 11, 0.4);
    border-radius: var(--radius-md);
    padding: 1.15rem 1.5rem;
    max-width: 800px;
    margin: 0 auto 2.25rem auto;
    text-align: left;
    backdrop-filter: blur(10px);
  }

  .warning-header {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--accent-amber);
    font-size: 0.85rem;
    font-weight: 800;
    letter-spacing: 0.03em;
    margin-bottom: 0.4rem;
  }

  .safety-warning-box p {
    color: var(--text-main);
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 0;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.85rem;
    margin-bottom: 3rem;
  }

  .via-gallery-grid {
    display: flex;
    justify-content: center;
    max-width: 440px;
    margin: 0 auto;
  }

  .phone-card {
    padding: 1.25rem;
    border-radius: 24px;
    text-align: left;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    border: 1px solid var(--border-glow);
  }

  .card-caption h4 {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--text-main);
    margin-top: 0.3rem;
  }

  .via-img {
    width: 100%;
    height: auto;
    border-radius: 16px;
    display: block;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.1rem;
    }
    .hero-actions {
      flex-direction: column;
    }
    .btn-primary, .btn-secondary {
      width: 100%;
      justify-content: center;
    }
  }
</style>
