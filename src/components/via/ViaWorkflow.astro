---
const steps = [
  {
    num: "1",
    title: "Acceso Profesional",
    icon: "🔑",
    desc: "Autenticación del facultativo sanitario mediante Firebase Auth. Cada evaluación queda firmada con su número de colegiado y perfil profesional.",
    badge: "Firma Acreditada",
    badgeColor: "cyan"
  },
  {
    num: "2",
    title: "Identificación del Paciente",
    icon: "👤",
    desc: "Búsqueda en el historial o alta sociodemográfica (NHC, edad calculada, sexo). Se genera la sesión clínica localmente en SQLite.",
    badge: "Datos Locales",
    badgeColor: "purple"
  },
  {
    num: "3",
    title: "Consentimiento Informado",
    icon: "✍️",
    desc: "Paso bloqueante legal: texto normativo y firma manuscrita digital del tutor sobre el lienzo táctil. Sin firma válida, la evaluación queda bloqueada.",
    badge: "Bloqueante Obligatorio",
    badgeColor: "rose"
  },
  {
    num: "4",
    title: "Pre-screening Clínico (CAP)",
    icon: "📋",
    desc: "Certificado de Aptitud para la Prueba: checklist de 4 capacidades. Si alguna no se cumple, bloquea la batería y sugiere métodos alternativos.",
    badge: "Aptitud Verificada",
    badgeColor: "rose"
  },
  {
    num: "5",
    title: "Sonómetro de Sala & Hub",
    icon: "🎛️",
    desc: "Comprobación del nivel de ruido en dB(A) (LAeq ≤ 45 dB), acceso a diagnóstico de audio y selección entre 7 lenguas de sesión.",
    badge: "Gate de Ruido & Idioma",
    badgeColor: "amber"
  },
  {
    num: "6",
    title: "Batería de 13 Módulos",
    icon: "🎮",
    desc: "Evaluación gamificada interactiva. El Modo Niño ofrece una interfaz lúdica; el clínico supervisa con mediciones deterministas y controles simultáneos.",
    badge: "13 Módulos",
    badgeColor: "emerald"
  },
  {
    num: "7",
    title: "Resultados & Telemetría QR",
    icon: "📊",
    desc: "Panel de resultados con semáforo clínico, generación de informe PDF estructurado y código QR de telemetría de usabilidad Zero-PHI (IEC 62366-1).",
    badge: "PDF + QR Anónimo",
    badgeColor: "cyan"
  },
  {
    num: "8",
    title: "Archivo y Seguimiento",
    icon: "🗂️",
    desc: "Persistencia local en el dispositivo del historial longitudinal de sesiones anteriores para seguimiento evolutivo del paciente.",
    badge: "Historial Clínico",
    badgeColor: "purple"
  }
];

const capItems = [
  { label: "Capacidad Visual", detail: "Identificación clara de estímulos gráficos y tarjetas de conjunto cerrado en la pantalla." },
  { label: "Motora Fina", detail: "Capacidad de interacción táctil (pulsación precisa, arrastre y silbato en tablet)." },
  { label: "Auditiva Periférica", detail: "Percepción basal suficiente para recibir consignas locutadas en la sala." },
  { label: "Estado Cognitivo", detail: "Alerta, colaboración y nivel de atención mínimos requeridos para tareas lúdicas." }
];
---

<section id="flujo-clinico" class="via-workflow section-padding">
  <div class="container">
    <div class="section-header">
      <span class="tag">Recorrido Secuencial Obligatorio</span>
      <h2>Flujo Clínico en Consulta <span class="gradient-text">& Puertas Bloqueantes</span></h2>
      <p>Cada fase constituye un prerrequisito ineludible para garantizar la validez metodológica y la seguridad del paciente pediátrico.</p>
    </div>

    <div class="timeline-grid">
      {steps.map((s) => (
        <div class="glass-card timeline-card">
          <div class="timeline-top">
            <span class="step-num">{s.num}</span>
            <span class="step-icon">{s.icon}</span>
            <span class={`badge badge-${s.badgeColor}`}>{s.badge}</span>
          </div>
          <h3>{s.title}</h3>
          <p>{s.desc}</p>
        </div>
      ))}
    </div>

    <!-- CAP Checklist Box -->
    <div class="glass-card cap-box">
      <div class="cap-header">
        <span class="cap-icon">🛡️</span>
        <div>
          <h3>Pre-Screening — Certificado de Aptitud para la Prueba (CAP)</h3>
          <p class="cap-sub">El facultativo debe verificar las 4 capacidades antes de habilitar la batería (paso bloqueante):</p>
        </div>
      </div>

      <div class="cap-grid">
        {capItems.map((item) => (
          <div class="cap-item">
            <span class="check-mark">✓</span>
            <div>
              <strong>{item.label}</strong>
              <span class="cap-detail">{item.detail}</span>
            </div>
          </div>
        ))}
      </div>
      
      <div class="cap-footnote">
        <em>*Excepción clínica:</em> El cribado de disfagia MECV-V no requiere CAP ni sonómetro de sala (el consentimiento informado sí es preceptivo).
      </div>
    </div>
  </div>
</section>

<style>
  .via-workflow {
    padding: 90px 0;
    position: relative;
  }

  .timeline-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3.5rem;
  }

  .timeline-card {
    display: flex;
    flex-direction: column;
    padding: 1.75rem;
    height: 100%;
    background: rgba(15, 23, 42, 0.65);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: var(--radius-lg);
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .timeline-card:hover {
    transform: translateY(-4px);
    border-color: rgba(0, 242, 254, 0.35);
  }

  .timeline-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
    gap: 0.5rem;
  }

  .step-num {
    font-family: var(--font-heading);
    font-size: 0.9rem;
    font-weight: 800;
    color: var(--accent-cyan);
    background: rgba(0, 242, 254, 0.1);
    width: 28px;
    height: 28px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(0, 242, 254, 0.3);
  }

  .step-icon {
    font-size: 1.4rem;
  }

  .timeline-card h3 {
    font-size: 1.15rem;
    font-weight: 800;
    margin-bottom: 0.6rem;
    color: var(--text-main);
  }

  .timeline-card p {
    font-size: 0.88rem;
    color: var(--text-muted);
    line-height: 1.5;
    margin: 0;
  }

  /* CAP Box */
  .cap-box {
    padding: 2.5rem;
    background: rgba(15, 23, 42, 0.75);
    border: 1px solid rgba(245, 158, 11, 0.3);
    border-radius: var(--radius-lg);
  }

  .cap-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.75rem;
  }

  .cap-icon {
    font-size: 2rem;
  }

  .cap-header h3 {
    font-size: 1.3rem;
    font-weight: 800;
    margin: 0 0 0.25rem 0;
    color: var(--text-main);
  }

  .cap-sub {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin: 0;
  }

  .cap-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.25rem;
    margin-bottom: 1.5rem;
  }

  .cap-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    background: rgba(2, 6, 23, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.05);
    padding: 1.25rem;
    border-radius: var(--radius-md);
  }

  .check-mark {
    color: #34d399;
    font-weight: 900;
    font-size: 1.1rem;
    background: rgba(16, 185, 129, 0.15);
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .cap-item strong {
    display: block;
    font-size: 0.95rem;
    color: var(--text-main);
    margin-bottom: 0.25rem;
  }

  .cap-detail {
    font-size: 0.82rem;
    color: var(--text-muted);
    line-height: 1.45;
    display: block;
  }

  .cap-footnote {
    font-size: 0.85rem;
    color: var(--text-dim);
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    padding-top: 1rem;
  }

  @media (max-width: 768px) {
    .via-workflow {
      padding: 60px 0;
    }
  }
</style>
