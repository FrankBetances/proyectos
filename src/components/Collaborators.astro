---
const baseUrl = import.meta.env.BASE_URL.endsWith('/') 
  ? import.meta.env.BASE_URL.slice(0, -1) 
  : import.meta.env.BASE_URL;

const collaborators = [
  {
    id: "starttic",
    name: "startTIC · Alta Tecnología",
    badge: "Incubación & Aceleración TIC",
    badgeColor: "cyan",
    desc: "Iniciativa del Consorcio Zona Franca de Vigo, Cámara PVV y GRADIANT, cofinanciada por FEDER vía Fundación INCYDE. Aceleración de MVPs, conectividad y validación tecnológica.",
    logo: "",
    logoBg: "dark",
    alt: "Logo startTIC - Incubadora de Alta Tecnología Vigo",
    customMark: true
  },
  {
    id: "acopros",
    name: "ACOPROS",
    badge: "Validación Logopédica & Clínica",
    badgeColor: "amber",
    desc: "Asociación de Colaboración y Promoción del Sordo (A Coruña). Revisión del corpus de voz, pares mínimos y módulo de LSE.",
    logo: `${baseUrl}/assets/collaborators/logo-acopros.png`,
    logoBg: "light",
    alt: "Logo ACOPROS - Asociación de Colaboración y Promoción del Sordo"
  },
  {
    id: "itemas",
    name: "ITEMAS / ISCIII",
    badge: "Sello de Calidad 2024",
    badgeColor: "cyan",
    desc: "Plataforma de dinamización e innovación de las capacidades industriales del Sistema Nacional de Salud (Instituto de Salud Carlos III).",
    logo: `${baseUrl}/assets/collaborators/logo-itemas.png`,
    logoBg: "light",
    alt: "Sello de Calidad ITEMAS ISCIII 2024 - Instituto de Salud Carlos III"
  },
  {
    id: "sgorl",
    name: "SGORL",
    badge: "Sociedad Científica Médica",
    badgeColor: "purple",
    desc: "Sociedad Gallega de Otorrinolaringología y Cirugía de Cabeza y Cuello. Aval científico y clínico en salud auditiva.",
    logo: `${baseUrl}/assets/collaborators/logo-sgorl.png`,
    logoBg: "dark",
    alt: "Logo SGORL - Sociedad Gallega de Otorrinolaringología"
  },
  {
    id: "acis",
    name: "ACIS - SERGAS",
    badge: "Agencia de Salud Pública",
    badgeColor: "emerald",
    desc: "Axencia de Coñecemento en Saúde (Servizo Galego de Saúde). Promoción del conocimiento e innovación asistencial en salud.",
    logo: `${baseUrl}/assets/collaborators/logo-acis.png`,
    logoBg: "light",
    alt: "Logo ACIS - Axencia de Coñecemento en Saúde (Servizo Galego de Saúde)"
  }
];
---

<section id="colaboradores" class="collaborators-section">
  <div class="container">
    <div class="section-header">
      <span class="tag">Acreditación & Respaldo Sanitario</span>
      <h2>Entidades Colaboradoras & <span class="gradient-text">Sellos de Calidad</span></h2>
      <p>
        Valeria+ cuenta con la validación de sociedades científicas, incubadoras de alta tecnología y agencias de innovación en salud.
      </p>
    </div>

    <div class="collab-grid">
      {collaborators.map((c) => (
        <div class={`glass-card collab-card ${c.id === 'starttic' ? 'collab-card-highlight' : ''}`}>
          <div class={`logo-container ${c.logoBg}`}>
            {c.customMark ? (
              <div class="starttic-brand-mark">
                <div class="starttic-top-row">
                  <span class="starttic-icon">🚀</span>
                  <span class="starttic-title">start<span class="highlight-tic">TIC</span></span>
                </div>
                <span class="starttic-sub">INCUBADORA DE ALTA TECNOLOGÍA</span>
                <span class="starttic-partners">CZFV · GRADIANT · CPVV · FEDER</span>
              </div>
            ) : (
              <img 
                src={c.logo} 
                alt={c.alt} 
                class={`collab-img img-${c.id}`} 
                loading="lazy" 
                decoding="async" 
              />
            )}
          </div>

          <div class="collab-body">
            <div class="collab-top">
              <h3 class="collab-name">{c.name}</h3>
              <span class={`badge badge-${c.badgeColor}`}>{c.badge}</span>
            </div>
            <p class="collab-desc">{c.desc}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .collaborators-section {
    padding: 90px 0;
    position: relative;
    background: linear-gradient(180deg, rgba(15, 23, 42, 0.6) 0%, rgba(10, 15, 26, 0.9) 100%);
    border-top: 1px solid var(--border-light);
  }

  .collab-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
    gap: 1.75rem;
  }

  .collab-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .collab-card:hover {
    transform: translateY(-4px);
    border-color: rgba(0, 242, 254, 0.35);
  }

  .collab-card-highlight {
    border-color: rgba(0, 242, 254, 0.35);
    background: linear-gradient(180deg, rgba(0, 242, 254, 0.04) 0%, rgba(15, 23, 42, 0.6) 100%);
  }

  .starttic-brand-mark {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    gap: 0.25rem;
    padding: 0.5rem;
  }

  .starttic-top-row {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .starttic-icon {
    font-size: 1.3rem;
  }

  .starttic-title {
    font-family: var(--font-heading);
    font-size: 1.55rem;
    font-weight: 900;
    letter-spacing: -0.03em;
    color: #FFFFFF;
  }

  .highlight-tic {
    background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--accent-blue) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .starttic-sub {
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    color: var(--accent-cyan);
    text-transform: uppercase;
  }

  .starttic-partners {
    font-size: 0.65rem;
    color: var(--text-muted);
    font-weight: 500;
    margin-top: 0.15rem;
  }

  .logo-container {
    height: 135px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.85rem 1.25rem;
    border-bottom: 1px solid var(--border-light);
  }

  .logo-container.light {
    background: #FFFFFF;
  }

  .logo-container.dark {
    background: linear-gradient(135deg, #090E17 0%, #0F172A 100%);
  }

  .collab-img {
    max-height: 85px;
    max-width: 85%;
    width: auto;
    object-fit: contain;
    transition: transform 0.25s ease;
  }

  .collab-card:hover .collab-img {
    transform: scale(1.05);
  }

  .img-itemas {
    max-height: 110px;
    max-width: 90%;
  }

  .img-acis {
    max-height: 64px;
    max-width: 85%;
  }

  .img-acopros {
    max-height: 75px;
    max-width: 90%;
  }

  .img-sgorl {
    max-height: 95px;
  }

  .collab-body {
    padding: 1.4rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }

  .collab-top {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.4rem;
    margin-bottom: 0.75rem;
  }

  .collab-name {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--text-main);
  }

  .collab-desc {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.55;
    margin: 0;
  }

  @media (max-width: 768px) {
    .collab-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
